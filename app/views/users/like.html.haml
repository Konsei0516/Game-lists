= render 'component/header'

.user-warrper
  %h1 ユーザープロフィール
  .user-box__side
    %p 
      = @user.name
    %p
      = @user.profile
    %p
      = @user.PSN_id
    %p
      = @user.friend_code
  .user-box__main
    %h2 お気に入り一覧
    %nav.user-select
      = link_to 'ユーザーレビュー', user_path(@user)
      = link_to("いいね一覧", "/users/#{@user.id}/like")
    .user-box__main__review
      = render 'like'
      = paginate @likes
  
  = link_to root_path do
    トップへ