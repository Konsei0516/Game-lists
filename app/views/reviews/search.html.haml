= render "component/header"

.search
  .search__side
    = render 'search_form'
    
    
  .search__main  
    .search__main__box  
      = render @reviews
    = paginate @reviews

  = link_to root_path do
    トップへ